# Build

This directory contains build artifacts for the Desk Electron application.

## Usage

Run `npm run build` to generate the production build, then `npm run package` to package the Electron app.
